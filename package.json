{
  "name": "string-utils",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc",
    "dev": "node dist/index.js",
    "start": "npm run build && npm run dev"
  },
  "devDependencies": {
    "typescript": "^5.0.4"
  }
}